@using RandevuSistemi.Models.Dto
@model List<DoktorVePoliklinikAdi>

@{
    ViewData["Title"] = "Doktorlar";
}

<header class="text-center py-4">
    <h1>SauSerdivan Hastanesi</h1>
    <p class="lead">Poliklinikler ve Doktorları</p>
</header>

@foreach (var item in Model)
{
    <div class="list-group mb-3">
        <a href="#" class="list-group-item list-group-item-action flex-column align-items-start">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">@(item.PoliklinikAdi)</h5>
            </div>

            <p class="mb-1">@item.doctor.AdSoyad</p>
        </a>
    </div>
    <style>
        .list-group-item:hover {
            background-color: #bd5d38; /* Burada istediğiniz arka plan rengini belirtebilirsiniz */
            cursor: pointer;
        }
    </style>
}


